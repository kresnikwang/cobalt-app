# 2025-06-12

## 项目状态审查 & P0/P1/P2 修复

### P0 — 桌面应用收尾
- ✅ 提交 3 个已修改文件（desktop: crash auto-restart, parallel queue, Svelte 5 fix）
- ✅ 构建验证：DMG 打包成功，`asarUnpack` 正确解包 ffmpeg-static 和 isolated-vm
- ✅ 核心功能审查：崩溃重启最多 3 次、队列调度器逻辑正确

### P1 — Bug 修复
- ✅ **stream/manage.js FIXME**: 替换为带错误处理的 Promise catch（MemoryStore 同步，Redis 异步有 catch）
- ✅ **FFmpeg runner 重构**: `startAttempts` 从全局变量改为 per-pipeline Map；移除 async setInterval
- ✅ **FFmpeg worker 错误码**: 新增 `out_of_memory`、`codec_error`、`format_error` 细粒度码
- ✅ **Stream 测试**: run-test.js 中实现了 tunnel URL 验证（参数完整性 + verifyStream 调用）

### P2 — HLS 支持
- ✅ 在 match-action.js 中移除了 HLS 本地处理的排除逻辑
- ✅ 添加详细的客户端 TODO（m3u8 下载 → 解析 → segment 获取 → ffmpeg WASM 合并）

### HLS 客户端本地处理（追加）
- ✅ 创建 `workers/hls.ts`：内联 m3u8 解析器（无需额外依赖），分段下载 & 拼接
- ✅ 创建 `runners/hls.ts`：Web Worker 生命周期管理 & 进度上报
- ✅ 更新 `queue.ts`：`createSavePipeline` 根据 `info.isHLS` 分支创建 HLS 或普通管道
- ✅ 更新 `run-worker.ts` & `types/workers.ts`：新增 `"hls"` worker 类型
- ✅ 更新 `match-action.js` TODO 注释

### 提交记录
1. `desktop: add crash auto-restart, parallel download queue, and fix Svelte 5 reactivity`
2. `api/stream: fix unhandled promise on streamCache.set, add stream testing, enable HLS local processing, improve ffmpeg error codes`
3. `web/hls: implement client-side HLS local processing pipeline`
4. `chore: CI pipeline, .env.example, i18n fixes, security hardening, api-client SDK, TODO cleanup`
5. `desktop: add i18n system, settings page, and remux page`
6. `test: add vitest unit test suite across api, web, and api-client`

### 上线差距补齐（追加）
- ✅ 创建 `.env.example`（完整环境变量文档）
- ✅ 创建 `.github/workflows/ci.yml`（API test + Web type-check/build + Desktop build）
- ✅ 实现 `packages/api-client/src/` SDK（类型定义 + CobaltClient + createCobaltClient）
- ✅ 桌面安全加固：启用 Electron sandbox、文件名 path.basename() 防路径穿越
- ✅ i18n 补齐：`auth.not_configured`(en+ru)、俄语 `star`/`follow`、全部 6 个空 `a11y/*.json`(20 key)
- ✅ 剩余 TODO 清理：移除 `externalProxy` 废弃引用、严格 remux MIME 白名单、`StrictPartial` 类型、数组元素校验

### 桌面应用功能补齐（追加）
- ✅ 创建 Svelte 5 响应式 i18n 系统（`i18n.ts` + en/ru JSON，45+ key，支持参数插值）
- ✅ 设置面板新增语言选择器（en/ru）
- ✅ 全部 UI 硬编码字符串替换为 `t()` 调用
- ✅ 新增 Remux 页面（标题 + 拖放区 + 支持格式说明）
- ✅ 新增顶部导航栏（Home / Remux 页签切换）

### 单元测试（追加）
- ✅ 安装 vitest 并配置 3 个包的测试环境
- ✅ **API (55 tests)**: MemoryStore、Crypto、Secrets、URL 处理
- ✅ **Web (25 tests)**: formatFileSize、ffmpegMetadataArgs、uuid、validateSettings
- ✅ **API Client (14 tests)**: CobaltClient、save、serverInfo、probeTunnel、session
- ✅ 总计 **94 tests, all passing**
